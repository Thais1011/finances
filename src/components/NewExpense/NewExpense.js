import React, { useState } from "react";
import "./NewExpense.css";
import ExpenseForm from "./ExpenseForm";

const NewExpense = (props) => {
  const [isEditing, setIsEditing] = useState(false);
  const saveExpenseDataHandler = (enteredExpenseData) => {
    const expenseData = {
      ...enteredExpenseData,
      id: Math.random().toString(),
    };
    props.onAddExpense(expenseData);
    setIsEditing(false);
  };
  const startEditingHandler = () => {
    setIsEditing(true);
  };
  const stopEditingHandler = () => {
    setIsEditing(false);
  };
  return (
    <div className="new-expense">
      {!isEditing && (
        <button onClick={startEditingHandler}>Add New Expense</button>
      )}
      {isEditing && (
        <ExpenseForm
          onSaveExpenseData={saveExpenseDataHandler}
          onCancel={stopEditingHandler}
        />
      )}
    </div>
  );
};
export default NewExpense;

////saveExpenseDataHandler está sendo pontuado apenas, e não chamado
//No  input nao precisava porque acontecia por tras já, não necessitava ser chamado por causa do onChange

//a função que está sendo declarada no newExpense, será executada na ExpenseForm(child).
//quando chamamos onSaveExpenseData no ExpenseForm, podemos passar como argumento o expenseData
//que é um parametro da função chamada pelo NewExpense
